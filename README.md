a {
  text-decoration: none;
}

<h1>personal_library_C</h1>

<img style="width: 30vw;" src="img\prog_c.jpg" alt="C code illustrative image"/>

<section>
<h2>What does each function do? | O que cada função faz?</h2>

<section>
<h3>Function <i>CheckNumberCharacters</i></h3>
<p>
Check how many times a character appears in a string. Where the first parameter receives the string and the second parameter the character (char) you want to find.
</p>

<p>
Verificar quantas vezes um caractere aparece em uma string. Onde o primeiro parâmetro recebe a string e o segundo parâmetro o caractere (char) que deseja encontrar.
</p>
</section>

<section>
<h3>Function <i>CheckNumberInSet</i></h3>

<p>
Checks if the string belongs to a numeric set. Where the first parameter is the string you want to parse, the second the set (str), for example "0123456789"
</p>
<p>
Verifica se a string pertence a um conjunto numérico. Onde o primeiro parâmetro é a string que deseja analizar, o segundo, o conjunto (str), por exemplo, "0123456789"
</p

</section>
<section>
<h3>Function <i>CheckStrValueToNumber</i></h3>

<p>
Checks if the string is a numeric value. The first parameter being the string you want to analyze. Thus, the function returns true or false (0 or 1).
</p>
<p>
Verifica se a string é um valor numérico. O primeiro parâmetro é a string que você deseja analisar. Assim, a função retorna verdadeiro ou falso (0 ou 1).
</p>

</section>
</section>


<section>
<h2>How to use the library? | Como usar a biblioteca?</h2>
<section>
  <p>Primeiro vamos chamar a biblioteca: <code>#include &lt;caminho do arquivo&gt;</code></p>
  <p>Após isso, basta chamar as funçõs e usá-las</p>
</section>

<section>
<p>First let's call the library: <code>#include &lt;file_path&gt;</code></p>
<p>After that, just call the functions and use them</p>
</section>

</section>

<a href="1-personal_library_C/personal_library.h" download="personal_library.h" type="text/h"> Download personal_library</a>